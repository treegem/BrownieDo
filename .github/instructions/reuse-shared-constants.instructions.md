---
applyTo: "**"
---

## Reuse shared constants

**Severity: MUST FIX**

Do not duplicate literal values or redefine existing constants. Use shared, named constants (or derive from a single source of truth) for values that must stay synchronized to avoid drift and improve maintainability.
